import React from "react";

const Button = () => {
  return (
    <div
      className={`bg-blackLight rounded-[8px] px-[31px] py-2 text-sm text-white`}
    >
      Sign In
    </div>
  );
};

export default Button;
